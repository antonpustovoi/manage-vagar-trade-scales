{
  "name": "manage-vagar-trade-scales",
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild index.js --bundle --platform=node --outfile=index-out.js"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "clipboardy": "^4.0.0",
    "esbuild": "^0.25.0",
    "serialport": "^13.0.0"
  }
}
